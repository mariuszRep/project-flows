-- Seed script to populate initial data
-- This file is run after schema.sql during container initialization

-- Insert default schema properties (will be loaded from schema_properties.json by the bootstrap script)
-- This is just a placeholder to ensure the script runs successfully
SELECT 'Database initialized successfully' as status;